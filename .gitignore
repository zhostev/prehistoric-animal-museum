node_modules/
dist/
dist-model-still/
cloudflare-dist/
coverage/
playwright-report/
test-results/
.vite/
.DS_Store
*.log

# Local handoff inputs from the private design workspace.
# The Goal may read these files, but must only commit reviewed runtime outputs.
.handoff/

# Private project workspace kept beside the product code for local reference.
# Keep assistant configuration private while publishing the reviewed,
# project-specific contributor skill for onboarding new animals.
.agents/*
!.agents/skills/
.agents/skills/*
!.agents/skills/prehistoric-animal-onboarding/
.wayfinder/
assets/candidates/
assets/review-generated/
docs/handoff/
docs/research/
docs/specification/
prototypes/
spikes/
tools/*

# The deterministic animal-onboarding toolchain is required by CI and by the
# production baseline/golden verifiers. Candidate runs and review evidence
# remain private under assets/candidates/.
!tools/animal-onboarding/
!tools/animal-onboarding/**
tools/animal-onboarding/**/__pycache__/
tools/animal-onboarding/**/*.py[cod]

# Local environment files.
.env
.env.*
!.env.example
